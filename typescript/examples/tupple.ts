// Typescript

// ----------------------------------------------------------------

// Tupple
// ? A tuple is a typed array with a pre-defined length and types for each index

type ColorSaturation = [string, number];

let colorSaturation: ColorSaturation = ["blue", 100];
let emotionRate: [string, number] = ["happy", 60];

// ----------------------------------------------------------------

// By: Nikko Abucejo

export {};
